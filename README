README: Open mHealth Client Libraries

These Javascript Libraries provide utilities that are broken down into two categories, 
Data Processing Units (DPUs) and Data Visualization Units (DVUs).

For convenience, this package provides a simple node.js and express based static server.
To run:

node server.js
 
